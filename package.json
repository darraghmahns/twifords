{
  "name": "thetwifordstrydc",
  "private": true,
  "type": "module",
  "description": "A private, multi-page D.C. restaurant guide and wedding gift for Jack & Anne.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test 'tests/**/*.test.js'",
    "build": "node scripts/build-pages.mjs",
    "predev": "npm run build",
    "pretest": "npm run build"
  },
  "dependencies": {
    "@vercel/functions": "^3.0.0"
  }
}
